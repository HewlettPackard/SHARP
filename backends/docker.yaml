# Configuration excerpt to use Docker backend
backend_options:
  docker:
    reset: |
      docker restart $FN
    run: |
      docker exec $FN $CMD $ARGS
    run_sys_spec: |
      docker exec $FN $SPEC_COMMAND
fn_path: "/app/"
