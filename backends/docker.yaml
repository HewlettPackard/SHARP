# Configuration excerpt to use Docker backend
# Uses docker run for ephemeral containers from images
backend_options:
  docker:
    reset: ""
    run: |
      docker run --rm $FN $ARGS
    run_sys_spec: |
      docker run --rm $FN $SPEC_COMMAND
fn_path: ""
