# I/O Microbenchmark Suite
# Defines 1 I/O-focused microbenchmark for memory/data movement patterns

include:
  - ../_shared.yaml

benchmarks:
  swapbytes:
    entry_point: ./swapbytes.py
    args: []
    tags: [io, memory]

sources: []  # Local I/O Python file

build:
  docker:
    base_image: python:3.10-slim
    requirements: [numpy]
  appimage:
    runtime: ubuntu:20.04

tags: [python, io]
